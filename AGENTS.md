# AGENTS 协作约定

## 1) 默认只读
- 默认模式为只读分析与文档整理。
- 未经明确授权，不进行代码改写（尤其是 Python 代码）与行为变更。

## 2) 改代码必须小步提交
- 如需修改代码，采用小步、可回滚、可验证的增量方式。
- 每一步只解决一个清晰问题，避免大范围混改。

## 3) 审计结论必须带证据
- 任何审计/评审结论必须包含：
  - 文件路径（必要时精确到行）；
  - 对应证据（代码片段、日志、命令输出要点）。
- 不接受无证据的主观结论。

## 4) 产物落地位置
- 新生成的报告、索引、交接材料默认放在 `docs/` 目录。
- 根目录仅放总览类文档（如 `ARCHITECTURE.md`）。
