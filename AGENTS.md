# AGENTS 协作约定

本文件定义 **AI 代理（如 Codex）与团队成员在本仓库中的协作规范**。
所有自动化代码修改、文档更新、验证设计与审计行为均应遵循本规范。

目标：

* 保证代码修改 **可控、可回滚、可验证**
* 保证项目 **可读性与可维护性**
* 保证验证结论 **有证据、可复现、可复核**
* 使仓库结构 **适合科研协作、技术交接与后续开源共享**

---

# 1) 工作模式与改动原则

默认模式为 **只读分析与文档整理**。

在未获得明确授权时：

* 不进行代码修改
* 不改变程序行为
* 不修改模型逻辑
* 不修改训练、评估、控制或仿真流程

允许执行：

* 代码阅读
* 审计分析
* 文档整理
* 架构说明
* 改进建议

获得授权后，仍必须遵循 **最小修改原则**：

* 每一步只解决 **一个明确问题**
* 每一步必须 **可回滚**
* 每一步必须 **可验证**

禁止：

* 大范围混改
* 多个问题一起修改
* 未验证的结构性重构
* 在当前阶段默认重新打开大规模训练搜索

推荐流程：

```text
分析问题
  ↓
提出最小 patch
  ↓
验证 patch
  ↓
记录修改原因
```

当前阶段尤其强调：

* 优先做 **最小闭环验证**
* 优先复用现有训练产物与配置契约
* 不把问题扩大成“完整系统重做”

---

# 2) 证据、验证与复杂任务执行

所有审计、评审或代码分析结论 **必须提供证据**。

证据至少应包含：

* 文件路径（必要时精确到行）
* 相关代码片段
* 命令输出
* 日志信息
* 产物路径

示例：

```text
src/uwnav_dynamics/eval/evaluate.py:120

当前评估已写出 masked horizon artifact，
对应产物位于 out/.../eval_test/rmse_by_horizon_masked.csv。
```

禁止：

* 无证据结论
* 纯主观推测
* 把“计划中的目标”描述成“已经实现的事实”

对于复杂任务，Codex 与协作者应优先：

1. **先计划**
2. **再分阶段实现**
3. **每阶段都给出运行命令、验证标准和风险点**

推荐格式：

```text
阶段目标：

最小实现：

运行命令：

验证标准：

风险点：
```

若任务跨越多个子系统，例如：

* 模型推理接入
* 控制循环
* ROS 2 / Gazebo 集成
* 可视化与指标记录

则必须先做阶段划分，再开始实现。

---

# 3) 当前阶段任务定义

本仓库原阶段重点是：

* 数据预处理
* 网络训练
* 离线评估
* 论文图示整理

当前阶段已切换为：

**验证已筛选出的深度学习动力学模型，能否承担“状态转移求解”职责，
作为仿真系统或控制器内部的短时动态预测部件使用。**

当前阶段的核心任务：

* 验证模型能否稳定执行短时 rollout / state transition
* 验证模型能否接入最小控制闭环或经验型仿真回路
* 验证接口、日志、指标与图表是否足以支持工程判断

当前默认主模型与模型切换约束：

* 默认主模型 family 仍是 `S1Predictor`
* 默认 rollout 契约仍是 `y_hat = y0 + cumsum(dY)`
* 当前控制/仿真验证默认从离线筛选后更接近主线的 `B4+U1` 候选开始；若改用 `U1` 或其他候选，必须明确写出原因与对照目的
* 若切换模型结构、block 组合、hidden size、rollout 语义或 checkpoint 来源，必须同时记录配置文件、权重路径、切换理由与对应评估证据
* 未形成清晰证据前，不要同时改模型结构和闭环验证框架

必须明确以下边界：

* 当前没有可信、完整、可直接用于高保真仿真的精确水动力学系数
* 不要把任务定义成“神经网络替代精确物理真值模型”
* 不要默认以高保真物理仿真为前置条件

当前阶段对模型的正确定位是：

* **控制器内部短时状态转移模型**
* **经验型仿真内核**
* **可用于闭环验证的学习型动态预测器**

因此：

* 可使用 ROS 2 / Gazebo，但它们优先作为 **集成壳层、消息框架或最小验证环境**
* 不应默认要求 Gazebo 提供高保真水动力学真值
* 不应把“缺少精确物理参数”当作停止当前阶段工作的理由

当前阶段完成标准（Done Criteria）至少包括：

* 具备一个 **最小可运行验证框架**
* 网络推理模块可以 **接入控制或仿真闭环**
* 能够记录输入、预测状态、控制量、关键指标和失败情形
* 能够复现实验步骤与运行命令
* 能够输出可用于论文和技术汇报复用的结果图

推荐的最小完成形态包括但不限于：

* `controller -> learned transition model -> next state -> logging`
* `policy / control law -> rollout loop -> metrics -> plots`
* `ROS 2 node -> model inference -> state update topic / service`

最小闭环验证建议优先顺序：

* 先做 **纯模型 rollout / replay 验证**
* 再做 **进程内最小控制闭环**
* 再做 **CLI 或 ROS 2 节点化封装**
* 最后再接 **Gazebo 或外部仿真壳层**

不要默认从“完整 ROS 2 + Gazebo 系统联调”开始。

当前阶段推荐记录的控制验证指标至少包括：

* `rmse_global` / `mae_global`
* `rmse_global_masked` / `mae_global_masked`
* `final_step` 误差
* `rollout_growth`
* `tail_error`（至少 `p95 / p99`）
* `worst_abs_bias` 与对应分量
* 有效步数、失败步数、非有限值触发次数
* 若进入闭环，还应记录推理延迟、循环周期与实际运行频率

当前阶段 **不要求默认实现**：

* 完整高保真水下物理世界
* 完整工程级控制系统全栈
* 实机部署级安全策略全集

---

# 4) 产物与文档落地规则

新生成的文档、报告、验证说明或索引应优先放置在：

```text
docs/
```

运行期实验产物、日志、指标和图表应优先沿用现有运行目录契约：

```text
out/
run.out_dir / run.variant
```

当前阶段新增代码的优先放置原则：

* 可复用的状态转移、推理适配、闭环验证 helper 优先放在 `src/uwnav_dynamics/`
* 用户入口、最小验证命令与编排逻辑优先放在 `src/uwnav_dynamics/cli/`
* 验证指标汇总、artifact 读写与结果整理优先复用 `src/uwnav_dynamics/eval/` 与 `src/uwnav_dynamics/experiment/`
* 新增结果图与绘图辅助优先放在 `src/uwnav_dynamics/viz/`
* `apps/dev/` 只放临时联调脚本，不承载需要长期维护的核心逻辑
* 只有在同类职责已经稳定且跨多个文件重复出现时，才新增新的一级功能目录

根目录原则上仅保留 **高层入口与工程元数据**，例如：

* README.md
* ARCHITECTURE.md
* LICENSE
* AGENTS.md
* CITATION.cff
* pytest.ini
* repo_tree.txt

不要把阶段性分析文档、临时报告或验证笔记散落在根目录。

文档职责划分如下：

| 文档 / 目录                   | 职责                  |
| --------------------------- | ------------------- |
| README.md                   | 项目入口                |
| ARCHITECTURE.md             | 系统架构说明              |
| docs/handover_guide.md      | 当前阶段交接入口            |
| docs/project_status.md      | 当前项目状态与风险           |
| docs/engineering_roadmap.md | 工程升级路线              |
| docs/modeling_roadmap.md    | 建模路线                |
| docs/evaluation_protocol.md | 训练评估与 artifact 契约    |
| docs/config_contract.md     | 配置系统契约              |
| docs/design/                | 工程契约与接口说明           |
| docs/math/                  | 理论与数学原理             |
| docs/thesis/                | 论文写作支撑材料           |
| docs/repo_index.md          | 文件索引与代码定位参考        |
| repo_tree.txt               | 仓库结构说明              |

说明：

* README 不包含复杂技术细节
* `handover_guide.md` 是当前交接第一入口
* `project_status.md` 负责说明当前阶段事实，不负责承载历史全部细节
* 理论推导仅放在 `docs/math/`
* 论文写作支撑材料与工程契约文档应分开维护

---

# 5) 代码说明与同步更新规范

所有新增或修改的代码文件 **必须在文件开头提供中文模块说明**，至少解释：

* 模块职责
* 主要功能
* 数据流
* 依赖模块

模板如下：

```python
"""
模块名称：

模块职责：
说明该模块在整个系统中的作用。

主要功能：
1.
2.
3.

数据流：
说明输入数据来自哪里，
模块内部如何处理，
输出数据流向哪里。

依赖模块：
列出关键依赖模块。

备注：
说明设计原因或实现约束。
"""
```

对于核心模块，例如：

* 数据对齐
* 数据集构建
* 模型训练
* 模型评估
* 状态转移验证
* 控制或仿真闭环接入

必须补充 **系统级数据流说明**，例如：

```text
IMU   (100 Hz)
DVL   (10 Hz)
PWM   (100 Hz)
Power (5 Hz)

        ↓

alignment
        ↓
dataset build
        ↓
training
        ↓
evaluation
        ↓
transition-model validation
        ↓
controller / simulator loop
```

如果代码行为发生变化，必须同步更新：

* 模块说明
* 数据流说明
* 参数说明
* 运行命令
* 验证标准

例如：

* 新增数据输入
* 新增处理步骤
* 修改输出格式
* 新增控制/仿真接口
* 修改状态转移模型输入输出约定

禁止：

```text
代码逻辑已改变
但注释仍描述旧逻辑
```

---

# 6) 理论文档与可视化要求

理论说明使用 **LaTeX 编写**，位置为：

```text
docs/math/
```

理论文档用于解释：

* 动力学模型
* 数据驱动建模
* 训练目标
* 控制接口
* 状态转移模型的数学表述

若理论文档描述的是“规划目标”而不是“已落地实现”，必须显式写明；
不得把尚未完成的闭环能力表述成已经验证成立。

当前阶段新增的图表、结果图和验证图，必须延续仓库已有风格。

参考基线包括：

* `src/uwnav_dynamics/viz/style/sci_style.py`
* `src/uwnav_dynamics/viz/style/imu_style.py`
* `docs/design/plot_style_guide.md`
* `docs/design/imu_plot_style.md`

图表要求：

* 偏学术风格
* 低饱和度
* 结构清晰
* 单位、图例、标题、坐标轴完整
* 适合论文和技术汇报复用

禁止：

* 高饱和、花哨、展示型大屏风格
* 颜色过多导致语义不清
* 缺少单位、图例或分组说明
* 与仓库现有图风明显割裂

优先做法：

* 复用已有 style helper
* 延续 `horizon / rollout / control_readiness` 等现有图的版式语言
* 新增控制或仿真验证图时，保持线型、配色、标注层次一致

---

# 7) 代理协作底线与项目目标

AI 代理（如 Codex）在本仓库中必须：

* 遵循本文件规范
* 不进行未授权代码修改
* 保持 patch 最小化
* 保持文档同步更新
* 保证修改可验证
* 在复杂任务中先计划，再分阶段落地

若发现：

* 旧文件缺少模块说明
* 注释与代码不一致
* 旧规范与当前仓库事实冲突

可以在 **不改变代码行为的前提下补充说明或修正规范**。

本仓库的总体目标是构建：

**科研级工程仓库（Research-grade Engineering Repository）**

特点：

* 可复现实验
* 可团队协作
* 可开源共享
* 可扩展研究
* 可承载从离线建模到最小闭环验证的连续工程链路

当前阶段的子目标是：

**把离线筛选后的学习型动力学模型，推进到“可验证的状态转移模型”阶段。**

项目协作始终遵循三条原则：

```text
清晰
可验证
可复现
```

所有代码、文档、实验结果与闭环验证结论均应满足这三点。

---
