# AGENTS 协作约定

本文件定义 **AI 代理（如 Codex）与团队成员在本仓库中的协作规范**。
所有自动化代码修改、文档更新与审计行为均应遵循本规范。

目标：

* 保证代码修改 **可控、可回滚、可验证**
* 保证项目 **可读性与可维护性**
* 使仓库结构 **适合科研协作与开源共享**

---

# 1) 默认只读

默认模式为 **只读分析与文档整理**。

AI 代理或协作者在未获得明确授权时：

* 不进行代码修改
* 不改变程序行为
* 不修改模型逻辑
* 不修改训练或评估流程

允许执行的操作包括：

* 代码阅读
* 审计分析
* 文档整理
* 架构说明
* 改进建议

任何代码修改必须 **明确授权**。

---

# 2) 改代码必须小步提交

当需要修改代码时，必须遵循 **最小修改原则**：

* 每一步只解决 **一个明确问题**
* 每一步必须 **可回滚**
* 每一步必须 **可验证**

禁止行为：

* 大范围混改
* 多个问题一起修改
* 未验证的结构性重构

推荐流程：

```
分析问题
  ↓
提出最小 patch
  ↓
验证 patch
  ↓
记录修改原因
```

---

# 3) 审计结论必须带证据

所有审计、评审或代码分析结论 **必须提供证据**。

证据必须包含：

* 文件路径（必要时精确到行）
* 相关代码片段
* 命令输出
* 日志信息

示例：

```
src/data_pipeline.py:135

当前 split 逻辑与 data.py:102 重复，
可能导致 train / eval split 不一致。
```

禁止：

* 无证据结论
* 纯主观推测

---

# 4) 产物落地位置

新生成的文档、报告或索引应放置在以下位置：

```
docs/
```

根目录仅保留：

* README.md
* ARCHITECTURE.md
* LICENSE
* AGENTS.md

说明：

* `docs/` 用于工程文档
* `docs/math/` 用于理论说明
* `docx/repo_tree.txt` 用于仓库结构说明

---

# 5) 代码文件说明规范（必须）

所有新增或修改的代码文件 **必须在文件开头提供模块说明**。

模块说明必须使用 **中文**，并解释：

* 模块职责
* 功能
* 数据流
* 依赖关系

目的：

* 帮助新人理解系统
* 提高代码可维护性
* 降低阅读成本

---

## 模块说明模板

每个 Python 文件开头必须包含以下结构：

```python
"""
模块名称：

模块职责：
说明该模块在整个系统中的作用。

主要功能：
1.
2.
3.

数据流：
说明输入数据来自哪里，
模块内部如何处理，
输出数据流向哪里。

依赖模块：
列出关键依赖模块。

备注：
说明设计原因或实现约束。
"""
```

---

# 6) 复杂模块必须包含系统级数据流说明

对于核心模块，例如：

* 数据对齐
* 数据集构建
* 模型训练
* 模型评估

必须在模块说明中提供 **系统级数据流示意**。

示例：

```
IMU   (100 Hz)
DVL   (10 Hz)
PWM   (100 Hz)
Power (5 Hz)

        ↓

alignment
        ↓
dataset build
        ↓
training
        ↓
evaluation
```

目的：

* 帮助开发者快速理解系统结构
* 减少跨模块阅读成本

---

# 7) 修改代码时必须同步更新说明

如果代码行为发生变化，必须同步更新：

* 模块说明
* 数据流说明
* 参数说明

例如：

* 新增数据输入
* 新增处理步骤
* 修改输出格式

禁止出现：

```
代码逻辑已改变
但注释仍描述旧逻辑
```

---

# 8) 文档职责划分

本仓库文档体系采用 **职责分离原则**。

| 文档                          | 职责      |
| --------------------------- | ------- |
| README.md                   | 项目入口    |
| ARCHITECTURE.md             | 系统架构说明  |
| docs/project_status.md      | 当前项目状态  |
| docs/engineering_roadmap.md | 工程升级路线  |
| docs/modeling_roadmap.md    | 建模路线    |
| docs/design/                | 工程契约说明  |
| docs/math/                  | 理论与数学原理 |
| docx/repo_tree.txt          | 仓库结构    |

说明：

* README 不包含复杂技术细节
* 理论推导仅放在 `docs/math/`

---

# 9) 理论文档规范

理论说明使用 **LaTeX 编写**。

位置：

```
docs/math/
```

理论文档用于解释：

* 动力学模型
* 数据驱动建模
* 训练目标
* 控制接口

示例公式：

水下机器人动力学：

[
M(\nu)\dot{\nu} + C(\nu)\nu + D(\nu)\nu + g(\eta) = \tau
]

数据驱动动力学：

[
x_{t+1} = f_\theta(x_t, u_t)
]

控制优化：

[
\min \sum ||x_k-x^*_k||_Q + ||u_k||_R
]

---

# 10) AI 代理协作要求

AI 代理（如 Codex）在修改仓库时必须：

* 遵循本文件规范
* 不进行未授权代码修改
* 保持 patch 最小化
* 保持文档同步更新
* 保证修改可验证

若发现：

* 旧文件缺少模块说明
* 注释与代码不一致

可以在 **不改变代码行为的前提下补充说明**。

---

# 11) 项目目标

本仓库目标是构建：

**科研级工程仓库（Research-grade Engineering Repository）**

特点：

* 可复现实验
* 可团队协作
* 可开源共享
* 可扩展研究

该规范旨在使本仓库成为：

**团队科研项目模板（Research Project Template）**

---

# 12) 协作原则

项目协作遵循三条原则：

```
清晰
可验证
可复现
```

所有代码、文档与实验结果均应满足这三点。

---